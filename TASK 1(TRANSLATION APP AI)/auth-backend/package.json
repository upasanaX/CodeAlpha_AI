{
  "name": "translation-auth-backend",
  "version": "1.0.0",
  "description": "Optional Auth & Saved Translations API for Translation App",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:init": "node -e \"const fs = require('fs'); const pool = require('./src/db'); pool.query(fs.readFileSync('schema.sql', 'utf8')).then(() => { console.log('Schema applied successfully'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
